// page user sees after they have entered the party size and uploaded receipt (takes them to insdie the session
// with the items listed for claiming)
export default function StartSession() {
  return (
    <>
      <p>session page</p>
    </>
  );
}
